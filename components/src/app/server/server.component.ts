import { Component } from "@angular/core";

@Component({
    selector: 'app-server', // Is the string used to insert the component
    templateUrl: './server.component.html' // The HTML template
})
//same name of the file
export class ServerComponent {

}